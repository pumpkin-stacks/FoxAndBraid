import React from "react";

const siteControl = (props) => {
  // initialize variables first

  // create sitewide functions here

  const value = {
    // put stuff here you want to be able to pass back after functions
  };
  return;
  <>
    <siteControl value={value}>{props.children}</siteControl>
  </>;
};

export default siteControl;
